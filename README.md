1.create an ec2 instance
2.clone this repo
3.execute ./install.sh (it will install docker, kind , kubectl)

4.execute "kind create cluster --config kind-config.yaml --name ts-kind-cluster" (it will create Kubernetes cluster)







