create an ec2 instance
clone this repo
execute ./install.sh (it will install docker, kind , kubectl)
execute "kind create cluster --config kind-config.yaml --name ts-kind-cluster" (it will create Kubernetes cluster)







